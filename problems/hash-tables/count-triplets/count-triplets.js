'use strict'

const HashTable = require('../hash-table');

/**
 * Given an array and a ratio r, determine the number of
 * triplets of indices such that the elements are in
 * geometric progression for the given ratio r.
 * @param {Number[]} arr 
 * @param {Number} r 
 * @returns {Number}
 */
function countTriplets(arr, r) {

}

module.exports = countTriplets;