"use strict";

const HashTable = require("../hash-table");

/**
 * Given a string, return the number of pairs of substrings
 * of the string that are anagrams of each other.
 * @param {String} str
 * @returns {Number}
 */
function getAnagrams(str) {}

module.exports = getAnagrams;
