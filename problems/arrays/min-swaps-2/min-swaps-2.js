"use strict";

/**
 * Given an unsorted array with no duplicates, determine
 * the minimum number of swaps required to sort the array
 * in ascending order.
 *
 * @param {Number[]} arr an array of numbers to sort
 * @returns {Number} the minimum number of swaps required
 *                   to sort the array.
 */
function minimumSwaps(arr) {}

module.exports = minimumSwaps;
