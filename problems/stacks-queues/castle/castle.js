'use strict'

/**
 * You are given a square grid with some cells open (.) and some blocked (X).
 * Given a start and end position, determine the number of moves it will
 * take to get to the end position.
 * @param {String[][]} grid 
 * @param {Number[]} start 
 * @param {Number[]} end 
 */
function minimumMoves(grid, start, end) {

}